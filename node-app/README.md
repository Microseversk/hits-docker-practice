# PhotoGallery - Node.js приложение

Веб-приложение для загрузки и просмотра фотографий, построенное на Node.js, Express и MySQL.

## 📋 Содержание

- [Архитектура](#архитектура)
- [Технологический стек](#технологический-стек)
- [Установка и запуск](#установка-и-запуск)
- [API Документация](#api-документация)
- [Структура проекта](#структура-проекта)
- [Тестирование](#тестирование)
- [Конфигурация](#конфигурация)

## 🏗️ Архитектура

Приложение следует архитектуре MVC (Model-View-Controller):

```
┌─────────────────┐
│   Client (Web)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Express Server  │
│   (app.js)       │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌──────────┐
│ Routes │ │  Views   │
│(index) │ │  (Pug)   │
└───┬────┘ └──────────┘
    │
    ▼
┌─────────────┐
│  Database   │
│   (MySQL)   │
└─────────────┘
```

### Компоненты:

1. **Routes** (`routes/index.js`) - Обработка HTTP запросов
2. **Views** (`views/*.pug`) - Шаблоны для рендеринга HTML
3. **Database** (`db.js`) - Управление подключением к MySQL
4. **Utils** (`utils/fileUtils.js`) - Вспомогательные функции
5. **Config** (`config/index.js`) - Конфигурация приложения

## 🛠️ Технологический стек

- **Backend Framework**: Express.js 4.16
- **Database**: MySQL 8.0
- **Template Engine**: Pug 2.0
- **File Upload**: express-form-data
- **Testing**: Jest + Supertest
- **Runtime**: Node.js 18+

## 🚀 Установка и запуск

### Запуск с Docker (рекомендуется)

**Требования:** Docker и Docker Compose

```bash
# Перейдите в директорию проекта
cd node-app

# Запустите приложение и базу данных
docker-compose up -d

# Приложение будет доступно по адресу: http://localhost:3000
```

**Остановка:**
```bash
docker-compose down
```

**Просмотр логов:**
```bash
docker-compose logs -f
```

### Локальный запуск (без Docker)

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Настройте базу данных MySQL:**
   - Создайте базу данных
   - Выполните скрипт `init.sql` для создания таблиц
   - Настройте переменные окружения (см. [Конфигурация](#конфигурация))

3. **Запустите приложение:**
   ```bash
   npm start
   ```

4. **Откройте в браузере:**
   ```
   http://localhost:3000
   ```

## 📡 API Документация

### GET `/`
Главная страница с галереей изображений.

**Ответ:** HTML страница с отображением всех загруженных изображений.

---

### GET `/new`
Страница формы для загрузки нового изображения.

**Ответ:** HTML форма для загрузки.

---

### POST `/new`
Загрузка нового изображения.

**Content-Type:** `multipart/form-data`

**Параметры:**
- `image` (file, required) - Файл изображения
- `name` (string, optional) - Название изображения
- `author` (string, optional) - Автор изображения
- `description` (string, optional) - Описание изображения

**Ограничения:**
- Максимальный размер файла: 10MB (настраивается через `MAX_FILE_SIZE`)
- Разрешенные типы: JPEG, PNG, GIF, WebP

**Успешный ответ (200):**
```json
{
  "success": true,
  "fileName": "uuid-generated-filename.jpg",
  "message": "Image uploaded successfully"
}
```

**Ошибки:**
- `400` - Отсутствует файл, превышен размер, неверный тип файла
- `500` - Внутренняя ошибка сервера

**Пример запроса (curl):**
```bash
curl -X POST http://localhost:3000/new \
  -F "image=@/path/to/image.jpg" \
  -F "name=My Photo" \
  -F "author=John Doe" \
  -F "description=A beautiful sunset"
```

---

### GET `/all`
API endpoint для получения всех изображений в формате JSON.

**Ответ (200):**
```json
[
  {
    "id": 1,
    "name": "Photo Name",
    "author": "Author Name",
    "description": "Description",
    "path": "filename.jpg",
    "date": "2025-12-28T10:00:00.000Z"
  }
]
```

**Ошибки:**
- `500` - Ошибка базы данных

**Пример запроса:**
```bash
curl http://localhost:3000/all
```

## 📁 Структура проекта

```
node-app/
├── bin/
│   └── www                 # Точка входа приложения
├── config/
│   └── index.js           # Конфигурация приложения
├── public/
│   ├── images/            # Загруженные изображения
│   └── stylesheets/       # CSS стили
├── routes/
│   └── index.js           # Маршруты приложения
├── tests/
│   ├── routes/            # Тесты маршрутов
│   └── utils/             # Тесты утилит
├── utils/
│   └── fileUtils.js       # Вспомогательные функции
├── views/
│   ├── index.pug          # Главная страница
│   ├── new.pug            # Форма загрузки
│   ├── layout.pug         # Базовый шаблон
│   └── error.pug          # Страница ошибки
├── app.js                 # Конфигурация Express
├── db.js                  # Подключение к БД
├── init.sql               # SQL скрипт инициализации
├── docker-compose.yml     # Docker Compose конфигурация
├── Dockerfile             # Docker образ приложения
└── package.json           # Зависимости проекта
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты с покрытием
npm test

# Тесты в режиме наблюдения
npm run test:watch
```

### Покрытие кода

Текущее покрытие:
- `utils/fileUtils.js`: 100%
- `routes/index.js`: ~56%
- `app.js`: ~83%

### Структура тестов

- **Unit тесты**: `tests/utils/` - тестирование утилит
- **Integration тесты**: `tests/routes/` - тестирование API endpoints

## ⚙️ Конфигурация

### Переменные окружения

Приложение использует следующие переменные окружения:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `3000` |
| `NODE_ENV` | Окружение (development/production) | `development` |
| `DB_HOST` | Хост базы данных | `localhost` |
| `DB_USER` | Пользователь БД | `mysql` |
| `DB_PASSWORD` | Пароль БД | `mysql` |
| `DB_NAME` | Имя базы данных | `db` |
| `MAX_FILE_SIZE` | Максимальный размер файла (байты) | `10485760` (10MB) |

### Пример .env файла

```env
PORT=3000
NODE_ENV=development
DB_HOST=localhost
DB_USER=mysql
DB_PASSWORD=mysql
DB_NAME=db
MAX_FILE_SIZE=10485760
```

## 🔒 Безопасность

- ✅ Параметризованные SQL запросы (защита от SQL-инъекций)
- ✅ Валидация типов файлов
- ✅ Ограничение размера файлов
- ✅ Использование UUID для имен файлов
- ✅ CORS настройки

## 📝 Лицензия

См. файл LICENSE в корне проекта.

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории проекта.
